import 'package:flutter/material.dart';

class AppSliverStacked extends StatelessWidget {
  const AppSliverStacked({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
